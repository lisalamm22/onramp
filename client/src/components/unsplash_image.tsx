import React, { Fragment } from 'react';
// import ImageListItem from '@material-ui/core/ImageListItem';

const UnsplashImage: React.FC<Props> = ({image, key}) => {
    return (
        <Fragment>
            {/* <ImageListItem ><img</ImageListItem> */}
        </Fragment>
    )
}

interface Props{
    image: any,
    key: number
}

export default UnsplashImage
